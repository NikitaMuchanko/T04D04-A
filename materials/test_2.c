#include <stdio.h>

int main(void) {
     int sum = 0;
     // you have to gange only string below
     for (int i = 1; i < 101; i++) {
          // NO CHANGE BELOW!
          sum += i;
     }
     printf("General answer: %d", sum);
}